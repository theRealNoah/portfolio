from django.shortcuts import render

# Create your views here.
from django.shortcuts import render

# Create your views here.
def timeline_index(request):
    return render(request, "timeline_home.html")
